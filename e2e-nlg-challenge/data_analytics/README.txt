get_n_slots.py: Returns a .csv file with the number of slots that the variable n_slots is set to
Also if pemrute is set to true it will also generate the file of permutations of the slots with slot number=n_slots

data_wrangling.py: more general python file that runs analytics on the data ( utterances. length etc..)

toJson: converts the existing csv files into "JSON" that the Wen et al implementatino accepts
